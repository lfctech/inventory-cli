[pytest]
addopts = -q --strict-markers
testpaths = tests
markers =
    unit: Mark a test as a unit test (mocked, no I/O).
filterwarnings =
    error
    # Allow known third-party deprecation noise. Add entries here when a
    # dependency upgrade introduces unavoidable warnings, with a comment
    # pointing at the upstream issue.
